# GitHub Models Provider

## Overview

The `GitHubModelsProvider` module provides access to free Large Language Models (LLMs) available through the GitHub Marketplace, specifically utilizing the Azure AI inference endpoint. This provider leverages a personal access token (PAT) for authentication and offers access to models like Llama, Mistral, and Phi, subject to GitHub's free tier token limits.

## Key Classes/Functions

